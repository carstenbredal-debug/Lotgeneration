[project]
name = "catalog-api"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pymssql>=2.2.0",
]

[tool.uvicorn]
app = "main:app"
